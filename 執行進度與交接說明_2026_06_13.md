# 執行進度與交接說明（2026-06-13）

> 這份文件是給「下一次對話」接續使用的完整交接筆記。因為這次對話 token 即將用完，
> 先在一個安全的段落暫停，把所有已知資訊、已完成的修改、架構決策、以及待辦清單
> 都寫在這裡，方便下一次直接接續，不需要重新分析。

---

## ⚠️⚠️⚠️ 最重要：目前程式碼「無法編譯」，千萬不要 push 到 GitHub ⚠️⚠️⚠️

目前已經對 `backend/prisma/schema.prisma` 做了修改（移除了 `DispatchRecord` model，
新增了 `DailyRoleRecord` model 與 `User.monthlyAllowance` 欄位），**但還沒有同步修改
所有引用 `DispatchRecord` 的程式碼**。

如果現在 push 上去，Railway 後端會建置失敗（TypeScript 編譯錯誤），網站會掛掉。

**還沒修好、會導致編譯失敗的兩個檔案：**

1. `backend/src/routes/dispatch.routes.ts`
   - 整支檔案都是操作 `prisma.dispatchRecord`（CRUD 派遣紀錄），這個 model 已被移除。
   - 這個檔案要整支改寫成「派遣統計頁」（需求 6，見下方待辦詳細說明）。

2. `backend/src/services/salaryService.ts`
   - 第 148~155 行左右：
     ```ts
     const dispatchRecords = await prisma.dispatchRecord.findMany({
       where: {
         date: { gte: monthStart, lt: monthEnd },
         OR: [{ driverId: userId }, { attendantId: userId }],
       },
     });
     const driverDays = dispatchRecords.filter((d) => d.driverId === userId).length;
     const attendantDays = dispatchRecords.filter((d) => d.attendantId === userId).length;
     ```
   - 要改成查詢新的 `prisma.dailyRoleRecord`：
     ```ts
     const dailyRoles = await prisma.dailyRoleRecord.findMany({
       where: {
         userId,
         date: { gte: monthStart, lt: monthEnd },
         role: { in: ["DRIVER", "ATTENDANT"] },
       },
     });
     const driverDays = dailyRoles.filter((r) => r.role === "DRIVER").length;
     const attendantDays = dailyRoles.filter((r) => r.role === "ATTENDANT").length;
     ```

**下次接續時，第一件事就是先修好這兩個檔案**（即使還沒做完需求 6 的完整派遣統計頁，
至少要讓 `dispatch.routes.ts` 改成暫時可編譯的版本，例如先回傳空陣列或基本統計，
讓整個專案能重新編譯成功），確保隨時都處於「可以安全 push」的狀態。

---

## 目前已完成的修改（尚未 commit / push）

執行 `git status --porcelain` 結果：

```
 M backend/prisma/schema.prisma
?? .claude/
?? DEPLOY_GUIDE.md
?? backend/prisma/migrations/20260613130000_supervisor_daily_role_and_allowance/
?? 需求12_重設密碼.md
?? 需求13_批次匯入送貨紀錄.md
?? 需求14_激勵獎金.md
?? 需求清單_2026_06_13.md
```

### 1. `backend/prisma/schema.prisma`（已編輯，內容如下，已是最終狀態）

主要變更：
- 新增 enum：
  ```prisma
  // 每日司機／隨車人員角色
  enum DailyRoleType {
    NONE      // 無
    DRIVER    // 司機
    ATTENDANT // 隨車人員
  }
  ```
- `User` model：
  - 新增欄位 `monthlyAllowance Float @default(0)` （固定每月職務加給，需求 11 用）
  - 新增關聯 `dailyRoles DailyRoleRecord[]`
  - 移除了 `driverDispatch` / `attendantDispatch` 兩個 `DispatchRecord[]` 關聯
- `MileageRecord` model：
  - 新增 `@@unique([userId, date, vehicleId])`（同一人同一天同一車輛只會有一筆紀錄，
    用於「覆蓋機制」/ upsert，需求 4 用）
  - 保留原本的 `@@index([vehicleId])` 與 `@@index([userId])`
- 新增 `DailyRoleRecord` model（需求 5 用）：
  ```prisma
  model DailyRoleRecord {
    id     String        @id @default(cuid())
    userId String
    user   User          @relation(fields: [userId], references: [id])
    date   DateTime
    role   DailyRoleType @default(NONE)

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@unique([userId, date])
  }
  ```
- **完全移除** `DispatchRecord` model
- `Vehicle` model：移除 `dispatchRecords DispatchRecord[]` 關聯

已執行 `cd backend && npx prisma generate`，**成功**（Generated Prisma Client v6.19.3），
證明 schema 語法正確、型別已重新產生（`prisma.dailyRoleRecord` 可用，
`prisma.dispatchRecord` 已經不存在）。

### 2. 新 migration 檔（已建立完成）

`backend/prisma/migrations/20260613130000_supervisor_daily_role_and_allowance/migration.sql`

完整內容：

```sql
-- CreateEnum
CREATE TYPE "DailyRoleType" AS ENUM ('NONE', 'DRIVER', 'ATTENDANT');

-- AlterTable: User - 新增固定每月職務加給
ALTER TABLE "User" ADD COLUMN     "monthlyAllowance" DOUBLE PRECISION NOT NULL DEFAULT 0;

-- CreateIndex: MileageRecord - 同一人同一天同一車輛僅一筆紀錄（覆蓋機制）
CREATE UNIQUE INDEX "MileageRecord_userId_date_vehicleId_key" ON "MileageRecord"("userId", "date", "vehicleId");

-- CreateTable
CREATE TABLE "DailyRoleRecord" (
    "id" TEXT NOT NULL,
    "userId" TEXT NOT NULL,
    "date" TIMESTAMP(3) NOT NULL,
    "role" "DailyRoleType" NOT NULL DEFAULT 'NONE',
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "DailyRoleRecord_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE UNIQUE INDEX "DailyRoleRecord_userId_date_key" ON "DailyRoleRecord"("userId", "date");

-- AddForeignKey
ALTER TABLE "DailyRoleRecord" ADD CONSTRAINT "DailyRoleRecord_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE RESTRICT ON UPDATE CASCADE;

-- DropTable: 改由 DailyRoleRecord + MileageRecord 統計派遣狀況，不再需要手動派遣紀錄
DROP TABLE "DispatchRecord";
```

> 注意：這個 migration 會 **DROP TABLE "DispatchRecord"**。這是早期系統，應該還沒有
> 正式派遣資料，但下次接續完成所有功能、要請使用者 push 之前，**務必再次提醒使用者
> 這個變更會刪除 DispatchRecord 資料表**（如果裡面有資料會永久消失）。

### 3. 其他未追蹤檔案
- `.claude/`、`DEPLOY_GUIDE.md`：與本次任務無關，不要動。
- `需求12_重設密碼.md`、`需求13_批次匯入送貨紀錄.md`、`需求14_激勵獎金.md`、
  `需求清單_2026_06_13.md`：四份需求文件本體，保留即可。

---

## 任務背景與總體規則

- 專案：物流員工管理系統，monorepo（`backend` = Node/Express/TS/Prisma/PostgreSQL，
  `frontend` = React/TS/Vite/Tailwind/react-router-dom/axios）。
- 部署在 Railway，GitHub repo：`https://github.com/devilish0616-blip/Delivery-record-Web`，
  push 後 Railway 會自動重新部署（後端會跑 `npx prisma migrate deploy`）。
- 使用者是技術小白，所有指令都要用 **Windows cmd 格式**。
- 本機 `backend/.env` 是 `DATABASE_URL="file:./dev.db"`（SQLite），但
  `schema.prisma` 的 datasource 是 `provider = "postgresql"`，**本機無法直接跑
  `prisma migrate dev` 或啟動連 DB 的後端**。只能用 `npx prisma generate`
  （不連線）來驗證 schema 語法與型別。一切以 Railway 上的版本為主，本機執行
  不用強求成功。
- 使用者已明確授權「執行 過程我全部Allow 不要在詢問」（全部直接做，不用每次問）。
- 規則：**一個功能一個功能來，完成一項再進行下一項**。每完成一項功能後，提醒使用者
  push 到 GitHub（但如上所述，目前專案處於編譯失敗的中間狀態，建議等到至少把
  dispatch.routes.ts 和 salaryService.ts 修好、且整個專案能 build 成功後，
  才進行第一次 push 與提醒）。

---

## 完整需求清單（共 14 項，含已知技術筆記）

### 需求 1：Bug — ADMIN 點「車輛管理」頁面整頁空白（白屏）

- **狀態：尚未修復，是下次接續的第一個功能性任務**（在修好 dispatch.routes.ts /
  salaryService.ts 的編譯問題之後）。
- 已花時間靠靜態程式碼閱讀 `frontend/src/pages/admin/VehiclesPage.tsx`（548 行，
  已在這次對話中完整讀過，內容見下方備註）、`backend/src/services/vehicleService.ts`、
  `backend/src/routes/vehicle.routes.ts`，**沒有找到 100% 確定的根因**。
- 已知線索：
  - `VehiclesPage.tsx` 會 render `v.maintenanceItems.map(...)`、
    `m.remaining.toFixed(0)`、`v.currentMileage`、`v.type`（用
    `typeLabels[v.type]` 查表）等欄位，這些都假設後端回傳的資料一定有這些欄位、
    且型別正確。
  - 討論紀錄（`討論紀錄_2026_06_13.md`）提到上一個 session 的 migration
    `20260613120000_vehicle_type_and_maintenance` 在 Railway DB 上「無法 100%
    確認是否已套用」。**如果這個 migration 還沒套用到正式 DB**，那麼
    `vehicleService.getVehicleStatus()` 查詢 `vehicle.maintenanceItems` 或
    `vehicle.type` 時，Prisma 可能會因為資料庫欄位/資料表不存在而拋錯，
    導致 `/api/vehicles` 回傳 500，前端 `apiClient.get("/vehicles")` 失敗，
    `catch` 會 `setError(getErrorMessage(err))`，理論上應該會顯示錯誤訊息文字
    而不是「完全空白」。但如果是某種「render 階段才噴錯且沒有 ErrorBoundary」的
    情況（例如 `v.maintenanceItems` 是 `undefined` 導致 `.map` 直接 throw），
    React 在沒有 ErrorBoundary 包裹的情況下，整個元件樹會被卸載，畫面就會變成
    全白（這完全符合「白屏」的症狀，而不是顯示錯誤文字）。
  - **計畫的修復方案**（下次直接做）：
    1. 在 `VehiclesPage.tsx` 對所有可能為 `undefined`/`null` 的欄位加防呆，例如：
       - `v.maintenanceItems ?? []`
       - `v.currentMileage ?? 0`
       - `m.remaining ?? 0`
       - `typeLabels[v.type] ?? v.type ?? "-"`（避免 `v.type` 不在
         `typeLabels` 裡時 `typeLabels[v.type]` 變成 `undefined` 而非崩潰——
         其實 `undefined` render 不會 crash，但保險起見還是處理）
    2. **新增一個全域 ErrorBoundary 元件**（例如
       `frontend/src/components/ErrorBoundary.tsx`），包在 `App.tsx` 的路由
       outlet 外層，這樣未來任何頁面 render 階段噴錯都會顯示「發生錯誤」訊息，
       而不是完全空白畫面——這對除錯與使用者體驗都很重要。
    3. 同時檢查 Railway 上的 migration 狀態：如果方便，可以在這次順便確認
       `20260613120000_vehicle_type_and_maintenance` 是否已套用（看
       `backend/prisma/migrations/` 目錄裡是否已經有這個檔案、且本次新 migration
       會接續在它後面跑 `prisma migrate deploy`，理論上 Railway 重新部署時
       會依序套用所有未套用的 migration，所以只要這次 push 後重新部署一次，
       兩個 migration 應該都會被套用）。

### 需求 2：新增 SUPERVISOR（主管）角色權限與導覽列重組

- 沿用既有的 `MANAGER` enum 值作為「主管」（不改 enum 名稱）。
- 權限：可以查看所有資料但不能修改，**除了**：
  (a) 可以編輯車輛保養項目（標記已更換、填寫備註）
  (b) 可以查看派遣紀錄（需求 6 的統計頁）
- 主管的指派方式：由 ADMIN 在「員工管理」頁面指派（跟 `specialTitle` 指派方式一樣，
  也就是 `PATCH /:id/role` 把角色設成 `MANAGER`）。
- **主管也要能填寫**：每日正/逆物流件數、每日車輛使用紀錄、每日司機/隨車人員角色
  （跟 EMPLOYEE 一樣）。
- 技術筆記：
  - `backend/src/middleware/auth.ts` 已有 `requireAdminOrManager`，註解已經寫
    「主管僅可查看，不可修改資料」，完全符合新需求的描述，**中介層基本不用改**，
    只需要在各 route 上正確套用。
  - 車輛保養項目相關 route（`PATCH /:id/maintenance/:itemId`、
    `POST /:id/maintenance`、`DELETE /:id/maintenance/:itemId`，都在
    `backend/src/routes/vehicle.routes.ts`）目前是 `requireAdmin`，**需要改成
    允許 MANAGER**（例如自訂一個 `requireAdminOrManagerForMaintenance` 或直接
    用 `requireAdminOrManager`，因為這三個操作正好符合「主管可編輯保養項目」）。
  - `frontend/src/layouts/AppLayout.tsx`：目前 `isStaff = role===ADMIN||MANAGER`
    會讓 MANAGER 完全只看到 `adminNav`（儀表板、薪資計算、派遣紀錄、貨運行對帳、
    車輛管理、員工管理、系統設定），**完全沒有員工自填頁面的入口**
    （每日送件記錄、車輛里程記錄、我的薪資）。需要重組導覽列，讓 MANAGER（以及
    之後 ADMIN，見需求 3）也能看到員工自填相關的連結。
  - `frontend/src/App.tsx` 的 `HomeRedirect` 與 `ProtectedRoute adminOnly`
    都已經把 MANAGER 當作 admin-like 處理；員工自填路由
    （`/delivery`、`/mileage`、`/salary/me`）本來就在非 `adminOnly` 的
    `ProtectedRoute` 裡，所以 ADMIN/MANAGER 其實已經可以「直接打網址」進去，
    只是沒有導覽連結。

### 需求 3：ADMIN 也能填寫自己的每日正/逆物流送件記錄

- 目前只有 EMPLOYEE 能進入填寫頁（`/delivery`，對應
  `frontend/src/pages/employee/DailyDeliveryPage.tsx`）。
- 需要讓 ADMIN（以及需求 2 的 MANAGER）的導覽列也出現「每日送件記錄」的連結，
  且該頁面對 ADMIN/MANAGER 也要能正常運作（後端 `POST /api/deliveries` 是用
  `requireAuth` + 自己的 `userId`，理論上任何已登入角色都能用，需要確認一下
  `backend/src/routes/delivery.routes.ts` 的 `POST /` 沒有限制角色）。
- 這跟需求 2 的導覽列重組是同一批改動，建議一起做。

### 需求 4：車輛里程/使用紀錄大改版（兩個欄位：機車必填 + 貨車選填）

- 「今日車輛使用」表單改為兩個區塊：
  - **欄位1（必填）= 機車**：從目前啟用中的機車車牌下拉選單選一台，填起/終里程。
  - **欄位2（選填）= 貨車**：同上，若當天沒用貨車可留空。
- **不再需要管理者手動建立派遣紀錄**，員工自行記錄車輛使用即可。
- 技術筆記：
  - 後端 `MileageRecord` 已加上 `@@unique([userId, date, vehicleId])`
    （已完成於本次的 schema migration），所以可以用 upsert
    （`prisma.mileageRecord.upsert`，where 用
    `userId_date_vehicleId` 複合鍵）達成「同一人同一天同一車輛只有一筆，
    可覆蓋」。
  - `backend/src/routes/mileage.routes.ts`（95 行，已讀過）：
    - 目前 `POST /` 是單純 `create`（沒有 upsert），`createSchema = {date,
      vehicleId, startMileage, endMileage}`。**需要改成 upsert**，且前端會送
      兩筆（機車一定送、貨車視情況送），或者後端設計成一次可以接收
      `motorcycle: {...}` 和 `truck?: {...}` 兩個物件、各自 upsert。
    - 會更新 `vehicle.currentMileage`（若 `endMileage > vehicle.currentMileage`）
      ——這個邏輯要保留。
  - `backend/src/routes/vehicle.routes.ts` 的 `GET /`：員工視角目前只回傳
    `{id, plateNumber, note}`，**缺少 `type` 欄位**，前端要能分出機車/貨車
    清單，所以這裡要把 `type` 加進回傳欄位，且應該只回傳 `isActive: true`
    的車輛（要確認目前是否已經過濾啟用中車輛）。
  - `frontend/src/pages/employee/MileagePage.tsx`（200 行，已讀過）：目前是
    單一車輛下拉（不分類型）+ 單一起訖里程表單，POST 到 `/mileage`（單筆
    create）。**需要整頁改版**：
    - 兩個區塊（機車必填、貨車選填），各自下拉選單只列出對應 `type` 的
      啟用車輛。
    - 表單送出時對「有填的欄位」呼叫 upsert API（機車一定有，貨車視是否填寫）。
    - 歷史紀錄表格也要能顯示車輛類型，方便使用者辨識。
  - `frontend/src/api/types.ts` 的 `Vehicle` / `MileageRecord` 型別要確認
    `type` 欄位存在且員工端 API 回傳有帶上。

### 需求 5：新增「今日角色」（司機/隨車人員）選擇

- 員工填寫每日紀錄時，新增「今日角色」下拉：無(None，預設) / 司機(Driver) /
  隨車人員(Attendant)。
- ADMIN 可以在後台修改任何人的「今日角色」。
- 技術筆記：
  - 對應新 model `DailyRoleRecord`（`@@unique([userId, date])`，已完成於本次
    schema migration），用 upsert。
  - 需要新的 route，例如 `backend/src/routes/dailyRole.routes.ts`（或併入
    `delivery.routes.ts` / `mileage.routes.ts`，待下次決定）：
    - `POST /api/daily-role`（或類似路徑）：自己 upsert 今天的角色，
      `requireAuth`，任何角色都能用（ADMIN/MANAGER/EMPLOYEE）。
    - `PUT /api/daily-role/:userId/:date`：ADMIN 修改任何人的角色
      （`requireAdmin`，類似 `PUT /api/deliveries/:userId/:date` 的 admin
      correction pattern）。
    - 記得在 `backend/src/index.ts` 註冊新 route。
  - 前端：`frontend/src/pages/employee/DailyDeliveryPage.tsx`
    （159 行，已讀過）是放「今日角色」選擇器最自然的地方（跟正/逆物流件數
    一起填）。

### 需求 6：派遣紀錄頁大改版（唯讀統計頁）

- **不再是管理者手動新增派遣紀錄**，改成「今天誰開哪台車、誰是司機、誰是隨車人員」
  的唯讀統計/總覽頁。
- 資料來源：員工自行記錄的車輛使用（`MileageRecord`，需求 4）+ 今日角色
  （`DailyRoleRecord`，需求 5）。
- ADMIN 與 SUPERVISOR(MANAGER) 都能看（需求 2 的權限規則）。
- 技術筆記：
  - `backend/src/routes/dispatch.routes.ts`（82 行，已讀過）**目前整支壞掉**
    （引用已移除的 `prisma.dispatchRecord`），這是優先要修的編譯錯誤。
  - 改寫方向：
    - `GET /api/dispatch?date=YYYY-MM-DD`（或 `?year=&month=`）：
      1. 查詢該日所有 `MileageRecord`（include `vehicle`、`user`），
         依 `vehicleId` 分組，得到「今天誰開了哪台車」。
      2. 查詢該日所有 `DailyRoleRecord`，得到每個人的角色（DRIVER /
         ATTENDANT / NONE）。
      3. 合併兩者，輸出例如：
         ```ts
         [
           {
             vehicleId, plateNumber, type,
             users: [
               { userId, name, role: "DRIVER" | "ATTENDANT" | "NONE",
                 startMileage, endMileage }
             ]
           },
           ...
         ]
         ```
    - 권限：`requireAuth, requireAdminOrManager`（ADMIN 可能還需要編輯角色的
      權限，MANAGER 純查看——但需求 2 說 SUPERVISOR 可以查看派遣紀錄，沒說
      可以編輯，所以編輯角色應該還是 `requireAdmin`，沿用需求 5 的
      `PUT /api/daily-role/:userId/:date`）。
  - `frontend/src/pages/admin/DispatchPage.tsx`（206 行，已讀過）**需要整頁
    重寫**：移除新增派遣表單與刪除按鈕，改成讀取上述統計 API 並渲染表格/卡片，
    ADMIN 可以點開某人那一列直接呼叫需求 5 的 admin 角色修改 API。

### 需求 7：車輛管理頁新增「使用歷史」展開區塊

- 管理者展開某台車輛時，顯示：
  - 里程紀錄歷史（誰、哪天、開了多少 km）—— 來自 `MileageRecord`。
  - 每日角色紀錄（誰記錄了司機/隨車人員）—— 來自 `DailyRoleRecord`
    （但 `DailyRoleRecord` 沒有 `vehicleId`，所以這裡應該是顯示「當天有使用
    這台車的人 + 他當天的角色」，需要 join `MileageRecord.userId/date` 對到
    `DailyRoleRecord.userId/date`）。
- 目的：幫助管理者決定是否要刪除/停用某台車輛。
- 技術筆記：
  - 後端可能需要新增 `GET /api/vehicles/:id/usage-history` 或在
    `vehicleService.getVehicleStatus()` 的回傳中加上
    `usageHistory: [{userId, userName, date, startMileage, endMileage,
    distance, role}]`（依日期排序，可能限制最近 N 筆或可分頁）。
  - 前端 `VehiclesPage.tsx` 的展開區塊（`expandedId === v.id` 那段）已經有
    保養項目表格，**在它旁邊或下方加一個新的「使用歷史」表格**。

### 需求 8：車輛類型可編輯 + 變更前確認對話框

- 編輯車輛時，**車型（機車/貨車）目前不可編輯**，需求要求改為可編輯。
- 變更前要彈出確認對話框：「變更車型將會重置預設保養項目，確定要修改嗎？」
  確認後才套用變更（會重新建立該車型的預設保養項目）。
- 技術筆記：
  - `backend/src/routes/vehicle.routes.ts` 的 `updateSchema` 目前不允許改
    `type`，需要：
    1. `updateSchema` 加上 `type: z.nativeEnum(VehicleType).optional()`
       （或類似寫法，要看現有 zod schema 風格）。
    2. 在 `PUT /:id` 的 handler 裡：如果 `type` 有變更且與原本不同，先
       `prisma.vehicleMaintenanceItem.deleteMany({where:{vehicleId: id}})`
       清掉舊保養項目，再依照 `vehicleService.ts` 的
       `DEFAULT_MAINTENANCE_ITEMS[newType]` 重新建立預設保養項目
       （`vehicleService.ts` 裡應該已經有建立預設項目的邏輯，建議抽成共用函式
       重複利用，例如建立車輛時也是呼叫同一個函式）。
  - 前端 `VehiclesPage.tsx` 的編輯區塊（`editingId === v.id`）：
    1. 把目前唯讀的 `<td>{typeLabels[v.type]}</td>` 改成下拉選單（跟新增表單
       一樣的 `<select>`，選項 TRUCK/MOTORCYCLE）。
    2. 新增 `editType` state，`startEdit` 時初始化為 `v.type`。
    3. `handleSaveEdit` 送出前，如果 `editType !== v.type`，先用
       `window.confirm("變更車型將會重置預設保養項目，確定要修改嗎？")`
       （或自訂 modal，看程式碼風格偏好，這個專案目前刪除車輛用的是自訂 modal
       不是 `window.confirm`，所以保養項目重置確認可能也要做成自訂 modal 比較一致）
       ，使用者取消的話就不送出 PUT 請求。

### 需求 9：修正 ADMIN 在薪資計算頁編輯他人每日件數的 bug（commit 3d5639e）

- **已 100% 確認根因**，是最簡單的一項，下次可以最快完成。
- `frontend/src/pages/admin/SalaryPage.tsx` 第 88 行附近：
  ```ts
  await apiClient.put(`/delivery/${userId}/${date}`, {
    forwardCount: editForward,
    reverseCount: editReverse,
  });
  ```
  錯誤：路徑是 `/delivery/...`（單數），但後端在 `backend/src/index.ts` 是
  `app.use("/api/deliveries", deliveryRoutes)`（**複數** `deliveries`），且
  `backend/src/routes/delivery.routes.ts` 裡確實有
  `PUT /:userId/:date`（`requireAdmin`，邏輯正確）。
- **修法**：把 `/delivery/${userId}/${date}` 改成 `/deliveries/${userId}/${date}`
  即可（一行字串修改）。

### 需求 10：薪資頁「匯出薪資單」單一員工 Excel

- 管理者在薪資頁選擇特定員工 + 月份，點「匯出薪資單」，產生該員工的 Excel
  薪資報表（出勤天數、每日件數明細、職稱判定、各項加給/獎金、實領薪資）。
- 技術筆記：
  - `backend/src/routes/salary.routes.ts`（174 行，已讀過）已有
    `GET /export`（`requireAdminOrManager`，產生全公司總表 + 各員工分頁的大
    Excel，使用 `exceljs`，`sanitizeSheetName` 處理工作表名稱）。
  - 建議新增 `GET /:userId/export`（`requireAdminOrManager`），重用
    `/export` 裡產生「單一員工分頁」的邏輯（可能需要把那段邏輯抽成共用函式
    `buildEmployeeSheet(workbook, employeeSalary, year, month)`），輸出
    只含這個員工的 Excel。
  - 前端 `frontend/src/pages/admin/SalaryPage.tsx`（446 行，已讀過）：在員工
    明細區塊加一個「匯出薪資單」按鈕，連結到
    `/api/salary/${userId}/export?year=...&month=...`（沿用現有 `/export`
    連結的寫法，`href` 直接連到 API）。
  - **此項依賴需求 11、14 的欄位（職務加給、激勵獎金）要先加進
    `EmployeeMonthlySalary` 與 Excel 產出邏輯**，建議排在 11、14 之後做。

### 需求 11：基礎設定新增「職務加給」區塊

- 設定頁（基礎設定）新增「職務加給」區塊：管理者可以針對特定員工設定固定的每月
  加給金額。
- 自動納入薪資計算，並顯示在薪資單上。
- 技術筆記：
  - 已在本次 schema migration 完成：`User.monthlyAllowance Float @default(0)`。
  - 後端：新增一個更新 endpoint，例如
    `PATCH /api/employees/:id/allowance`（`requireAdmin`，body:
    `{monthlyAllowance: number}`），放在 `backend/src/routes/employee.routes.ts`
    （該檔案已有 `PATCH /:id/role`、`PATCH /:id/special-title`、
    `PATCH /:id/status`、`POST /:id/title-override` 等類似 pattern，可以照抄）。
  - `salaryService.ts`：在 `EmployeeMonthlySalary` interface 加上
    `jobAllowance: number`（= `user.monthlyAllowance`），並加進
    `totalSalary` 計算。
  - 前端 `frontend/src/pages/admin/SettingsPage.tsx`（307 行，已讀過）：
    新增「職務加給」區塊，列出所有員工 + 目前的 `monthlyAllowance` 數字輸入框 +
    儲存按鈕（逐筆 PATCH，或批量送出，看現有設定頁其他區塊的寫法決定風格）。
  - 前端薪資頁與 Excel 匯出要顯示這個欄位（與需求 14 一起處理）。

### 需求 12：員工管理頁新增「重設密碼」

- 每個員工列表旁新增「重設密碼」按鈕。
- 點擊跳出確認 modal：新密碼、確認密碼兩個欄位，不一致顯示錯誤，成功顯示
  「密碼已更新」。
- 後端：新增 `PUT /api/employees/:id/password`，**僅 ADMIN**，bcrypt hash
  （沿用現有 pattern，`backend/src/routes/auth.routes.ts` 裡
  `bcrypt.hash(password, 10)`）。
- 技術筆記：
  - `backend/src/routes/employee.routes.ts`（已讀過）：在現有的
    `PATCH /:id/role` 等 route 旁邊新增：
    ```ts
    const passwordSchema = z.object({
      password: z.string().min(6), // 視現有密碼規則決定最小長度
    });

    router.put("/:id/password", requireAdmin, async (req, res) => {
      const parsed = passwordSchema.safeParse(req.body);
      if (!parsed.success) return res.status(400).json({ error: "..." });
      const passwordHash = await bcrypt.hash(parsed.data.password, 10);
      await prisma.user.update({
        where: { id: req.params.id },
        data: { passwordHash },
      });
      res.json({ success: true });
    });
    ```
    需要在檔案最上方 `import bcrypt from "bcrypt"`（確認現有 import 風格，
    可能是 `bcryptjs`，要看 `auth.routes.ts` 怎麼 import）。
  - 前端 `frontend/src/pages/admin/EmployeesPage.tsx`（163 行，已讀過）：
    - 每列新增「重設密碼」按鈕，點擊開啟一個 modal（這個專案在
      `VehiclesPage.tsx` 已有自訂 modal 的寫法可以參考其樣式）。
    - modal 內：新密碼 input、確認密碼 input、錯誤訊息顯示、確認按鈕。
    - 送出前檢查兩次密碼是否一致，一致才呼叫
      `PUT /api/employees/${id}/password`，成功後顯示「密碼已更新」
      （可以用簡單的 `alert` 或頁面內提示文字，看現有風格）。

### 需求 13：管理者批次匯入過往送貨紀錄（Excel）

- 位置：後台「每日送件記錄」管理頁面（目前對應頁面待確認——可能是需求 3
  之後新增的、ADMIN 可見的送件記錄管理頁，或是直接擴充
  `frontend/src/pages/employee/DailyDeliveryPage.tsx` 在 ADMIN 視角下顯示
  額外按鈕；下次接續時需要先確認「後台每日送件記錄管理頁」實際指的是哪個頁面
  ——目前專案裡似乎還沒有一個「管理者可以看到所有人送件紀錄」的專屬頁面，
  可能需要新建一個，或者在 `SalaryPage.tsx` / 新建
  `frontend/src/pages/admin/DeliveryRecordsPage.tsx` 裡加入)。
- 新增「批次匯入」按鈕，點擊跳出 modal：
  1. 提供 Excel 範本下載按鈕
  2. 上傳 Excel 檔案
  3. 預覽解析結果（筆數、員工、日期範圍）
  4. 確認匯入
  5. 結果顯示：成功幾筆、失敗幾筆（含原因）
- Excel 欄位：員工姓名 or Email（必填，姓名優先比對，找不到再用 Email）、
  日期 YYYY/MM/DD（必填）、正物流件數（必填）、逆物流件數（必填）、備註（選填）。
- 後端：`POST /api/delivery/batch-import`（注意：需求文件寫的是
  `/api/delivery/batch-import` 單數，但目前 delivery route 是掛在
  `/api/deliveries` 複數——下次實作時要注意路徑一致性，建議掛在
  `/api/deliveries/batch-import`，前端對應呼叫同樣路徑即可，不用糾結需求文件
  字面上的單複數），**僅 ADMIN**，用 `(userId, date)` upsert（沿用
  `DeliveryRecord` 的 `@@unique([userId, date])`）。
- 技術筆記：
  - 後端會需要：
    - `exceljs` 解析上傳的檔案（`multer` 或類似套件處理檔案上傳——需要確認
      專案目前是否已有檔案上傳的 middleware，若沒有需新增）。
    - 範本下載：可以是 `GET /api/deliveries/batch-import/template`，用
      `exceljs` 產生一個只有表頭（員工姓名、日期、正物流件數、逆物流件數、
      備註）+ 一行範例資料的 Excel 檔回傳。
    - 匯入邏輯：逐列解析 → 依「姓名」在 `User` 表找 `name` 比對，找不到再用
      `email` 比對 → 找不到則記錄失敗原因（例如「找不到員工：XXX」）；
      日期格式 `YYYY/MM/DD` 要 parse 成 `Date`（注意時區，參考
      `backend/src/utils/date.ts` 的 `parseDateOnly`）；正/逆物流件數要是
      數字，否則記錄失敗原因；都通過則 `prisma.deliveryRecord.upsert`
      （where: `userId_date`，update/create 都寫入 `forwardCount`,
      `reverseCount`, `note`）。
    - 回應格式：`{ successCount, failureCount, failures: [{row, reason}] }`。
  - 前端：
    - 範本下載按鈕直接連結到後端範本 API 或前端用 `exceljs`/`xlsx` 在前端
      產生（看專案是否已有前端 Excel 套件依賴，若沒有優先用後端產生比較簡單）。
    - 上傳檔案後先在前端用 `xlsx`/`exceljs` 解析做「預覽」，或直接把檔案
      POST 到一個「預覽」API 再顯示結果——為了簡化，**可以考慮直接上傳給後端，
      後端解析完直接回傳預覽資料 + 一個暫存 token，使用者確認後再呼叫一次
      正式匯入**，但這樣較複雜；另一個簡化方案是**前端用 `xlsx` 套件直接在
      瀏覽器解析檔案做預覽，使用者確認後把解析出的 JSON 陣列（不是檔案本身）
      POST 給 `/api/deliveries/batch-import`**，這樣後端 API 直接接收 JSON
      陣列即可，不需要處理檔案上傳/multer，**這個方案比較適合這個專案的複雜度
      與使用者技術水平，建議採用**。

### 需求 14：薪資計算新增「激勵獎金」

- 規則（IF/ELSE，>60 優先判定，不會兩個都加）：
  - 出勤天數 ≥ 25 **且** (正物流+逆物流合計)/出勤天數 > 60 → **+3000**
  - 出勤天數 ≥ 25 **且** 平均 > 30 → **+1500**
  - 其他 → 0
- 加入 `totalSalary`。
- 前端：薪資頁員工明細顯示「激勵獎金」欄位；Excel 匯出（總表 + 個人分頁）也要
  包含此欄位。
- 不需要設定頁（規則固定寫死在程式碼）。
- 技術筆記：
  - `backend/src/services/salaryService.ts`（209 行，已讀過）：
    - `EmployeeMonthlySalary` interface（第 23~45 行附近）加上
      `incentiveBonus: number`。
    - 在 `calculateEmployeeMonthlySalary` 裡，已經會算出 `attendanceDays`
      （出勤天數）與每日 `forwardCount`/`reverseCount` 加總，計算：
      ```ts
      const totalCount = /* 正物流+逆物流合計，應該已有現成變數 */;
      const avgPerDay = attendanceDays > 0 ? totalCount / attendanceDays : 0;
      let incentiveBonus = 0;
      if (attendanceDays >= 25 && avgPerDay > 60) {
        incentiveBonus = 3000;
      } else if (attendanceDays >= 25 && avgPerDay > 30) {
        incentiveBonus = 1500;
      }
      ```
    - 把 `incentiveBonus` 加進 `totalSalary` 的加總公式裡。
  - 前端 `SalaryPage.tsx`：員工明細表格新增「激勵獎金」欄位顯示
    `s.incentiveBonus`。
  - `GET /export` 的 Excel 產出（總表 sheet + 個人 sheet）都要新增
    「激勵獎金」欄位/列。

---

## 待辦清單（TodoWrite 狀態，下次接續請先還原這個清單）

1. ~~Schema migration: DailyRoleRecord, MileageRecord unique key,
   monthlyAllowance, drop DispatchRecord~~ — **schema.prisma 已編輯完成、
   migration SQL 已寫好、`npx prisma generate` 成功。標記為 completed。**
2. **【下次第一步，新增的緊急任務】修復編譯錯誤**：
   - `backend/src/routes/dispatch.routes.ts`（整支引用已移除的
     `prisma.dispatchRecord`）
   - `backend/src/services/salaryService.ts`（第 148~155 行引用
     `prisma.dispatchRecord`）
   - 目標：讓專案重新可以 `tsc` 編譯成功（不一定要做完需求 6/14 的完整功能，
     但至少要讓型別正確、不引用已刪除的 model）。
3. Bug fix #1：ADMIN 車輛管理頁面白屏（加防呆 + ErrorBoundary）
4. 需求 9：修正 `/delivery` → `/deliveries` 路徑 bug（一行修改，最簡單）
5. 需求 12：員工重設密碼
6. 需求 8：車輛類型可編輯 + 確認對話框 + 保養項目重置
7. 需求 2：SUPERVISOR(MANAGER) 角色權限 & 導覽列重組
8. 需求 3：ADMIN 可填寫每日送件記錄（導覽列入口）
9. 需求 4：車輛使用紀錄大改版（機車必填 + 貨車選填）
10. 需求 5：每日司機/隨車人員角色紀錄
11. 需求 6：派遣紀錄頁改為統計頁（與步驟 2 的修復整合為完整功能）
12. 需求 7：車輛管理頁使用歷史
13. 需求 11 & 14：職務加給 + 激勵獎金（與步驟 2 的 salaryService 修復整合為完整功能）
14. 需求 10：單一員工薪資匯出
15. 需求 13：批次匯入送貨紀錄
16. 最終：前後端 build 檢查（`cd frontend && npm run build`、
    `cd backend && npx tsc --noEmit` 或對應的 build script）、總結、
    提醒使用者 push 到 GitHub（**並提醒 DispatchRecord 資料表將被刪除**）

---

## 已讀過、有筆記的關鍵檔案（不需要重新讀，除非要確認最新內容）

- `backend/prisma/schema.prisma` — 已編輯完成（見上方）
- `backend/src/middleware/auth.ts` — `requireAdmin`, `requireAdminOrManager`
  （註解已符合 SUPERVISOR 規格，不太需要改）
- `backend/src/index.ts` — 所有 route 註冊處，`/api/dispatch`,
  `/api/deliveries`（複數）等
- `frontend/src/App.tsx` — `HomeRedirect`, `ProtectedRoute adminOnly` 把
  MANAGER 當 admin-like
- `frontend/src/auth/ProtectedRoute.tsx` — adminOnly 檢查邏輯
- `frontend/src/layouts/AppLayout.tsx` — `employeeNav` / `adminNav` /
  `isStaff`，需求 2/3 要改這裡
- `frontend/src/api/types.ts` — 各種型別定義，之後要新增
  `DailyRole`/`DailyRoleType`、更新 `EmployeeMonthlySalary`
  （加 `jobAllowance`, `incentiveBonus`）、更新 `User`
  （加 `monthlyAllowance`）、`MileageRecord`/`Vehicle`（確認 `type`）
- `backend/src/routes/vehicle.routes.ts` — `GET /`（員工視角缺 `type`）,
  `updateSchema`（不能改 `type`）, 保養項目 routes（目前 `requireAdmin`，
  需求 2 要開放 MANAGER）, `DELETE /:id`（引用
  `prisma.dispatchRecord.count(...)`，**這裡也要修！**——之前的分析提到
  `DELETE /:id` 裡有 `dispatchCount` 檢查，這也會編譯失敗，要一併處理）
- `backend/src/services/vehicleService.ts` — `DEFAULT_MAINTENANCE_ITEMS`,
  `getVehicleStatus`, `listVehicleStatuses`
- `frontend/src/pages/admin/VehiclesPage.tsx` — 548 行，完整讀過（見上方
  bug #1 分析）
- `backend/src/routes/employee.routes.ts` — `requireAdminOrManager` +
  `PATCH /:id/role`, `PATCH /:id/special-title`, `PATCH /:id/status`,
  `POST /:id/title-override`；需要新增 `PUT /:id/password`（需求12）、
  `PATCH /:id/allowance`（需求11）
- `backend/src/routes/delivery.routes.ts` — 126 行，掛在 `/api/deliveries`，
  有 `POST /`, `PUT /:userId/:date`（requireAdmin）, `GET /`, `GET /summary`；
  需求13 要新增 batch-import
- `frontend/src/pages/admin/EmployeesPage.tsx` — 163 行，需求12要加重設密碼
  按鈕+modal
- `backend/src/routes/mileage.routes.ts` — 95 行，需求4要改 upsert + 兩欄位
- `backend/src/routes/dispatch.routes.ts` — 82 行，**目前壞掉**，需求6要重寫
- `backend/src/routes/salary.routes.ts` — 174 行，有 `/export`；需求10要加
  `/:userId/export`
- `frontend/src/pages/employee/DailyDeliveryPage.tsx` — 159 行，需求5要加
  今日角色選擇器；需求13批次匯入入口可能放這裡（或新建管理頁）
- `frontend/src/pages/employee/MileagePage.tsx` — 200 行，需求4要整頁改版
- `frontend/src/pages/admin/DispatchPage.tsx` — 206 行，需求6要整頁重寫
- `frontend/src/pages/admin/SalaryPage.tsx` — 446 行，需求9一行修正、需求
  10/11/14 要擴充
- `frontend/src/pages/admin/SettingsPage.tsx` — 307 行，需求11要加職務加給區塊
- `backend/src/services/salaryService.ts` — 209 行，**目前壞掉**
  （`prisma.dispatchRecord`），需求11/14要擴充
- `backend/src/routes/dashboard.routes.ts` — 117 行，用到
  `calculateAllEmployeesMonthlySalary()`，需求11/14改完後會自動反映在
  dashboard 總額（因為它是把每個人的 `totalSalary` 加總）
- `frontend/src/pages/admin/DashboardPage.tsx` — 185 行，跟 VehiclesPage
  類似 render 邏輯，bug#1 修好後可考慮順手檢查
- `backend/src/utils/date.ts` — `parseDateOnly`, `startOfMonth`,
  `startOfNextMonth`, `toDateOnlyString`，之後新功能會重複用到

---

## 環境注意事項

- Bash tool 的工作目錄會在呼叫之間保留。之前發生過 `cd backend && ...` 之後
  忘記切回根目錄，導致後續指令找不到檔案的問題。**下次每次執行 bash 指令前，
  先確認/設定正確的工作目錄**（Read/Edit/Write/Glob 用絕對路徑不受影響，
  但 Bash 相對路徑會受影響）。
- 專案根目錄絕對路徑：`c:\Users\user\Desktop\VS Code\New`
  （Bash 裡對應 `/c/Users/user/Desktop/VS Code/New`）。
- Node v24.16.0 / npm 11.13.0 已確認可用；Docker 不可用。
- `cd frontend && npm run build` 在開始本次修改前可以成功（92 modules,
  140ms），可作為之後驗證的 baseline。
- 後端目前沒有對應的本機 build/驗證指令可以連 DB 跑（SQLite vs PostgreSQL
  不一致），驗證 schema 正確性用 `npx prisma generate`；驗證程式碼型別正確性
  可嘗試 `cd backend && npx tsc --noEmit`（如果 `tsconfig.json` 設定允許）。

---

## 下次接續時建議的開場動作

1. 讀這份文件（已經在讀了）。
2. 先處理「待辦清單」第 2 項：修復 `dispatch.routes.ts`、
   `salaryService.ts`、以及 `vehicle.routes.ts` 裡 `DELETE /:id` 的
   `dispatchCount` 檢查，讓專案恢復可編譯狀態。
   - `dispatch.routes.ts` 可以直接做需求6的完整重寫（一次到位），或先做一個
     最小可編譯版本（回傳基於 `MileageRecord`/`DailyRoleRecord` 的簡單統計），
     之後需求6再來精修——**建議直接做完整版本**，因為需求5（DailyRoleRecord
     的 CRUD route）也會在這附近，兩者可以一起設計 API。
   - `salaryService.ts` 的修復建議跟需求11/14一起做（一次把
     `jobAllowance`、`incentiveBonus`、`driverDays`/`attendantDays` 改法
     都處理掉），這樣只改一次這個檔案。
3. 之後依照清單剩餘項目，一個一個完成、隨時用
   `cd frontend && npm run build` 驗證前端、必要時用
   `cd backend && npx tsc --noEmit`（或專案的 build script）驗證後端。
4. 全部完成後，做最終 build 檢查，然後給使用者一次完整的總結 +
   **push 到 GitHub 的提醒（cmd 格式指令）+ DispatchRecord 表將被刪除的提醒**。

---

（文件結束）
